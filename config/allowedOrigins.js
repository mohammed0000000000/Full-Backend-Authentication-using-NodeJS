// origin => true -> mean that the any domain name can access
// any Domain name you want to acess add it in allowOrigins array
const allowOrigins = ['http://localhost:5000']


module.exports = allowOrigins;